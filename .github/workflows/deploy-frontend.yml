name: Deploy Frontend (Streamlit)

on:
  push:
    branches:
      - main
    paths:
      - "frontend/**"
      - ".github/workflows/deploy-frontend.yml"
  workflow_dispatch:

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: us-central1
  SERVICE_NAME: realestate-frontend
  ARTIFACT_REGISTRY_REPO: realestate-repo

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      # ---------------------------------------------------------------
      # Step 1: Checkout Code
      # ---------------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # ---------------------------------------------------------------
      # Step 2: Authenticate to Google Cloud
      # ---------------------------------------------------------------
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # ---------------------------------------------------------------
      # Step 3: Setup gcloud CLI
      # ---------------------------------------------------------------
      - name: Setup gcloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      # ---------------------------------------------------------------
      # Step 4: Configure Docker for Artifact Registry
      # ---------------------------------------------------------------
      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker us-central1-docker.pkg.dev -q

      # ---------------------------------------------------------------
      # Step 5: Generate correct REPO PATH
      # ---------------------------------------------------------------
      - name: Set Repo Path
        run: echo "REPO=us-central1-docker.pkg.dev/${PROJECT_ID}/${ARTIFACT_REGISTRY_REPO}" >> $GITHUB_ENV

      # ---------------------------------------------------------------
      # Step 6: Get Backend URL
      # ---------------------------------------------------------------
      - name: Get Backend URL
        id: backend-url
        run: |
          BACKEND=$(gcloud run services describe realestate-backend \
            --region=${{ env.REGION }} \
            --format="value(status.url)" 2>/dev/null || true)

          if [ -z "$BACKEND" ]; then
            echo "⚠ Backend not found. Using local URL."
            BACKEND="http://localhost:8080"
          fi

          echo "url=$BACKEND" >> $GITHUB_OUTPUT

      # ---------------------------------------------------------------
      # Step 7: Build Docker Image
      # ---------------------------------------------------------------
      - name: Build Frontend Docker Image
        run: |
          docker build \
            -f frontend/Dockerfile.frontend \
            -t $REPO/${{ env.SERVICE_NAME }}:${{ github.sha }} \
            -t $REPO/${{ env.SERVICE_NAME }}:latest \
            frontend

      # ---------------------------------------------------------------
      # Step 8: Push Image
      # ---------------------------------------------------------------
      - name: Push Frontend Image
        run: |
          docker push $REPO/${{ env.SERVICE_NAME }}:${{ github.sha }}
          docker push $REPO/${{ env.SERVICE_NAME }}:latest

      # ---------------------------------------------------------------
      # Step 9: Deploy to Cloud Run
      # ---------------------------------------------------------------
      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image=$REPO/${{ env.SERVICE_NAME }}:${{ github.sha }} \
            --region=${{ env.REGION }} \
            --platform=managed \
            --allow-unauthenticated \
            --memory=2Gi \
            --cpu=2 \
            --timeout=300 \
            --min-instances=1 \
            --max-instances=10 \
            --set-env-vars="BACKEND_API_URL=${{ steps.backend-url.outputs.url }}"

      # ---------------------------------------------------------------
      # Step 10: Get URL
      # ---------------------------------------------------------------
      - name: Get Frontend URL
        id: service-url
        run: |
          URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region=${{ env.REGION }} \
            --format="value(status.url)")
          echo "url=$URL" >> $GITHUB_OUTPUT

      # ---------------------------------------------------------------
      # Step 11: Validate Deployment
      # ---------------------------------------------------------------
      - name: Validate Deployment
        run: |
          echo "Waiting for service..."
          sleep 10
          curl -f ${{ steps.service-url.outputs.url }} || exit 1

      # ---------------------------------------------------------------
      # Step 12: Summary
      # ---------------------------------------------------------------
      - name: Deployment Summary
        run: |
          echo "==============================================="
          echo "🎉 Frontend Deployment Complete!"
          echo "🌐 URL: ${{ steps.service-url.outputs.url }}"
          echo "📦 Image: $REPO/${{ env.SERVICE_NAME }}:${{ github.sha }}"
          echo "🔗 Backend API: ${{ steps.backend-url.outputs.url }}"
          echo "==============================================="
