# ============================================================================
# GITHUB ACTIONS - Auto Deploy Frontend on Push
# ============================================================================
# PURPOSE: Deploy Streamlit frontend to Cloud Run on every push to main.
# ============================================================================

name: Deploy Frontend (Streamlit)

on:
  push:
    branches:
      - main
    paths:
      - "frontend/**"
      - ".github/workflows/deploy-frontend.yml"
  workflow_dispatch:

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: us-central1
  SERVICE_NAME: realestate-frontend
  REPO: us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/realestate-repo

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      # ---------------------------------------------------------------
      # Step 1: Checkout Code
      # ---------------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # ---------------------------------------------------------------
      # Step 2: Authenticate to Google Cloud
      # ---------------------------------------------------------------
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # ---------------------------------------------------------------
      # Step 3: Setup gcloud CLI
      # ---------------------------------------------------------------
      - name: Setup gcloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      # ---------------------------------------------------------------
      # Step 4: Configure Docker to push to Artifact Registry
      # ---------------------------------------------------------------
      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker us-central1-docker.pkg.dev -q

      # ---------------------------------------------------------------
      # Step 5: Get Backend URL (fallback to local for dev)
      # ---------------------------------------------------------------
      - name: Get Backend URL
        id: backend-url
        run: |
          BACKEND=$(gcloud run services describe realestate-backend \
            --region=${{ env.REGION }} \
            --format='value(status.url)' 2>/dev/null || true)

          if [ -z "$BACKEND" ]; then
            echo "⚠ Backend not found. Using default local URL."
            BACKEND="http://localhost:8080"
          fi

          echo "url=$BACKEND" >> $GITHUB_OUTPUT

      # ---------------------------------------------------------------
      # Step 6: Build Docker Image
      # ---------------------------------------------------------------
      - name: Build Frontend Docker Image
        run: |
          docker build \
            -f frontend/Dockerfile.frontend \
            -t $REPO/${{ env.SERVICE_NAME }}:${{ github.sha }} \
            -t $REPO/${{ env.SERVICE_NAME }}:latest \
            frontend

      # ---------------------------------------------------------------
      # Step 7: Push to Artifact Registry
      # ---------------------------------------------------------------
      - name: Push Frontend Image
        run: |
          docker push $REPO/${{ env.SERVICE_NAME }}:${{ github.sha }}
          docker push $REPO/${{ env.SERVICE_NAME }}:latest

      # ---------------------------------------------------------------
      # Step 8: Deploy to Cloud Run
      # ---------------------------------------------------------------
      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image=$REPO/${{ env.SERVICE_NAME }}:${{ github.sha }} \
            --platform=managed \
            --region=${{ env.REGION }} \
            --allow-unauthenticated \
            --memory=2Gi \
            --cpu=2 \
            --timeout=300 \
            --min-instances=1 \
            --max-instances=10 \
            --set-env-vars="BACKEND_API_URL=${{ steps.backend-url.outputs.url }}"

      # ---------------------------------------------------------------
      # Step 9: Retrieve Deployed URL
      # ---------------------------------------------------------------
      - name: Get Frontend URL
        id: service-url
        run: |
          URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region=${{ env.REGION }} \
            --format='value(status.url)')
          echo "url=$URL" >> $GITHUB_OUTPUT

      # ---------------------------------------------------------------
      # Step 10: Test Deployed Service
      # ---------------------------------------------------------------
      - name: Validate Deployment
        run: |
          echo "Waiting for service to warm up..."
          sleep 10
          curl -f ${{ steps.service-url.outputs.url }} || exit 1

      # ---------------------------------------------------------------
      # Step 11: Deployment Summary
      # ---------------------------------------------------------------
      - name: Deployment Summary
        run: |
          echo "==============================================="
          echo "🎉 Frontend Deployment Complete!"
          echo "🌐 URL: ${{ steps.service-url.outputs.url }}"
          echo "📦 Image: $REPO/${{ env.SERVICE_NAME }}:${{ github.sha }}"
          echo "🔗 Backend API: ${{ steps.backend-url.outputs.url }}"
          echo "==============================================="
